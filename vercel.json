{
  "buildCommand": "npm run build && npx esbuild server/trpcEntry.ts --bundle --platform=node --packages=external --format=esm --outfile=api/trpc/\\[trpc\\].mjs",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "functions": {
    "api/trpc/[trpc].mjs": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/trpc/(.*)", "destination": "/api/trpc/$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
